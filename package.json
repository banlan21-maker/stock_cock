{
  "name": "stock-cock",
  "private": true,
  "scripts": {
    "dev": "concurrently -n front,back -c blue,green \"npm run dev:front\" \"npm run dev:back\"",
    "dev:front": "frontend\\run-dev.cmd",
    "dev:back": "cd backend && python -m uvicorn app.main:app --reload --reload-include \".env\" --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
